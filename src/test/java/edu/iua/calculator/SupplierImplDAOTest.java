package edu.iua.calculator;

import java.text.SimpleDateFormat;

import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

public class SupplierImplDAOTest {
	
//    static SimpleDateFormat dateFormat;
//    static int id;
//
//	@BeforeAll
//	void setup() {
//    	dateFormat = new SimpleDateFormat("yyyy-MM-dd");
//    	id = 4;
//	}
	
	@Test
	@Disabled
	void findBillingByIdSuccess() {
//		Billing billingExpected = new 
//		Billing billingObtained = (Billing) FactoryDAO.getInstance().getBillingDAO().findById(id);
//	    
//    	assertEquals(billingObtained.getAmount(), billingExpected.getAmount());
//    	assertEquals(
//        		dateFormat.format(billingObtained.getDate()), 
//        		dateFormat.format(billingExpected.getDate()));
    }
	
	@Test
	@Disabled
	void findBillingByIdFail() {
//		Billing billingObtained = (Billing) FactoryDAO.getInstance().getBillingDAO().findById(id);
//	    
//    	assertNotEquals(billingObtained.getAmount(), billingExpected.getAmount());
//    	assertNotEquals(
//        		dateFormat.format(billingObtained.getDate()), 
//        		dateFormat.format(billingExpected.getDate()));
    }

}